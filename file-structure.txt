discord-bot/
├── .env                          # Environment variables (token, DB URL, etc.)
├── package.json
├── index.js                      # Entry point — bot bootstrapping only
│
├── config/
│   ├── constants.js              # Thresholds, grace periods, scoring weights
│   └── roles.js                  # Instructor/admin role IDs or names
│
├── events/                       # Discord.js event handlers
│   ├── ready.js                  # onReady
│   ├── messageCreate.js          # onMessage (commands, participation)
│   ├── voiceStateUpdate.js       # onVoiceStateUpdate (voice attendance)
│   └── ...                       # future: interactionCreate, etc.
│
├── commands/                     # Command logic (manual triggers)
│   ├── start-session.js          # !start-session handler
│   ├── end-session.js            # !end-session handler
│   ├── summary.js                # /summary or !summary
│   └── ...                       # future: !ping, !help, etc.
│
├── services/                     # Business logic (shared by commands & schedulers)
│   ├── sessionManager.js         # startSession(), endSession(), getActiveSession()
│   ├── attendanceTracker.js      # Voice-based attendance logic
│   ├── participationTracker.js   # Message counting & scoring
│   └── summaryService.js         # Store/retrieve session summaries
│
├── models/                       # Database models / schemas (when DB is added)
│   ├── Session.js                # Session schema
│   ├── Attendance.js             # Attendance records
│   └── Participation.js          # Participation records
│
├── utils/                        # Helpers
│   ├── validators.js             # Channel type, permissions, session state checks
│   ├── logger.js                 # Simple logger (console for now, file later)
│   └── dateUtils.js              # Timestamp comparisons, duration calculations
│
├── dashboard/                    # Future Next.js dashboard (or separate repo)
│   └── (empty for now)
│
└── tests/                        # Unit tests (optional but scalable)
    └── sessionManager.test.js